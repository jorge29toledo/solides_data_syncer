# frozen_string_literal: true

RSpec.describe SolidesDataSyncer do
  it "has a version number" do
    expect(SolidesDataSyncer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
